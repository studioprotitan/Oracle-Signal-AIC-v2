# MOAI MISSION-CRITICAL HANDOFF: HOLOGRAPHIC COGNITIVE ENGINE

This confidential operational handoff documents structural layout systems, digital signal algorithms, core assets, and active engineering states. It is compiled to ensure frictionless continuum transition for incoming elite system operators.

---

## 🌌 System Overview & Brand Spec

The **Holographic Cognitive Engine** is an immersive, military-grade scientific terminal simulating sub-spatial spectral analysis and quantum synthesis of recovered Abyssum artifacts. 

- **Aesthetic DNA**: Clean Swiss/Modern cyberpunk interface set on an onyx-dark canvas (`#0c090a`). Uses glowing cyber-cyan (`#00f0ff`), deep amethyst purple, and gold/caramel alert structures.
- **UI Architecture**: Immersive Glassmorphism HUD rows, heavy status telemetry readouts, micro-borders, typing-effect feedback ticks, and interactive SVG/D3 charting modules.
- **Sensory Modeling**: Synchronizes visual and physical simulations with custom synthesized low-frequency soundscapes via the Web Audio API.

---

## 📂 Codebase Geography

```
├── App.tsx                   # Core State Machine, UI layout, layout managers & tab indices
├── types.ts                  # Absolute data definitions, structural interfaces, & metrics schemas
├── DEV_LOG.md                # Development cycle log tracking telemetry updates & physics upgrades
├── MOAI_HANDOFF.md           # [THIS FILE] System Handoff documenting codebase architecture
├── index.html                # Entry HTML injection hub
├── index.css                 # Inter-font typography core, Tailwind directives & glowing effects
├── components/
│   ├── LoreNetworkGraph.tsx  # Dynamic SVG connections network visualizer with interactive link nodes
│   ├── AugmentedCanvas.tsx   # Holographic visualizer projecting spatial distortions and water ripples
│   ├── VariantRadarChart.tsx # Precise D3.js radar chart comparing Active vs Base signal metrics
│   ├── RiftIncursionScanner.tsx # Matrix-style visual locator measuring signal decay & corruption alerts
│   ├── OriginMiniMap.tsx     # Vector grid mapping orbital coordinates & signal sector regions
│   ├── TripoMeshWireframe.tsx# Interactive matrix representing relic wireframe structural models
│   ├── AudioVisualizer.tsx   # Real-time HTML5 Canvas rendering Web Audio spectrum wave-frames
│   ├── ArchiveCompareChart.tsx# Historic analytics plotter for archive records comparatives
│   ├── ResonanceHeatmap.tsx  # Interactive sensor heat thermal matrix of physical excitation points
│   ├── RepairBay3D.tsx       # Calibration module illustrating 3D grid line intersections
│   ├── LoadingState.tsx      # Atmospheric startup booting terminal interface with fake diagnostic logs
│   └── widgets/
│       └── WidgetEngine.tsx  # Auxiliary console control panels and tactile modulation controllers
└── services/
    └── geminiService.ts      # Google Gen AI SDK integration for high-fidelity relic lore synthesis
```

---

## 🎛️ Key Technical Architectures

### 1. Physical Physics & Wave Models
To simulate natural fluid mechanics during a **VEO-3 Pulse** discharge, the engine utilizes an asymmetric physical wave envelope:

$$E(t) = \begin{cases} \sin\left(\frac{t}{0.20} \cdot \frac{\pi}{2}\right) & t < 0.20 \\ e^{-4.5(t - 0.20)} & t \ge 0.20 \end{cases}$$

- **Phase rise**: Represents rapid, explosive ionization build-up peaking in exactly $500\text{ ms}$ (at $t = 0.20$ of the $2500\text{ ms}$ master duration).
- **Physical Decay Tail**: Smoothly tapers the ripple intensity $E(t)$ using an exponential decay curve with a damping constant of $4.5$, preventing any mechanical snapping at the animation boundary.
- **Continuous System Drift**: The $E(t)$ envelope modulates both:
  1. **Scrub Position Indicator**: $x(t) = \lfloor E(t) \times 40 \rfloor + 50$ (sweeps fluidly up to $90\%$ before gracefully settling back to center $50\%$).
  2. **Pulse Modulator Coefficient ($pulseModulator$)**: Modulates visual water ripple translation amplitude dynamically, sliding to flat water at sequence conclusion.

### 2. Relational Coaxial Network Graph
Located in the Lore Glossary Modal (`loreViewMode === 'graph'`), `<LoreNetworkGraph>` dynamically charts relations between Abyssum definitions:
- **Inter-node Physics**: Simulates nodes as a spring-mass network. Core definitions hover gracefully with customizable vector coordinates.
- **Categorized Visual Filters**: Highlights historic decrypted items with interactive glowing red nodes, and core telemetry definitions with amber focus states. Hover states pull linked definition logs on-screen in real time.

### 3. Integrated Audio Wave Synth
Uses three parallel Web Audio context oscillators (Fundamental, fifth interval, octave overtone) synchronized to the VEO-3 pulse.
- **Pitch Ramp**: Features a logarithmic sweep peaking alongside visual peak energy ($1.1\text{ s}$).
- **Butterworth Filter Sweep**: Opens a structural lowpass filter ($320\text{ Hz} \to 1550\text{ Hz}$) concurrently to visualize extreme quantum density.

---

## 🎮 Operational Checkpoints & Controls

1. **Intake & Drop Zone**: Grab an image or click to load a local PNG/JPEG file. The validated layout handles error rejection instantly.
2. **Frequency Calibration**: Rotate sliders to alter frequency ($1\text{ Hz}$ to $100\text{ Hz}$).
3. **Pulse Trigger**: Engage **VEO-3 Pulse** for full visual-physical-audio integration. Modulators sweep, telemetry logs print in real-time, and the physical decay tail settles smoothly.
4. **Lore Glossary**: Toggle between the **Alphabetical Index** and **Resonance Graph** via top headers in the right console drawer.

---

*Handoff complete. All diagnostics are currently nominal. Ready for operational continuation.*
